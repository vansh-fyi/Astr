// Platform-specific initialization for web
// Web doesn't need display mode or other mobile-specific features

Future<void> initializePlatformSpecific() async {
  // No platform-specific initialization needed for web
  return;
}
